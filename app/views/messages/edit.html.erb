<%= turbo_frame_tag @message do %>
  <%= form_for [@room, @message] do |f| %>
    <%= f.text_area :content %>
    <%= f.submit "Edit",
                 class: "w-[40px] text-center text-white bg-blue-400 hover:bg-blue-600 rounded-xl p-0.5 my-2"
    %>
  <% end %>
<% end %>